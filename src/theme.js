import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#28282b', // замените на ваш цвет
    },
    secondary: {
      main: '#9c27b0',
    },
  },
});

export default theme;